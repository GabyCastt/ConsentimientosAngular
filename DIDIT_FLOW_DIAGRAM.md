# 🔄 Diagrama de Flujo DIDIT Free/Premium

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FLUJO DE VERIFICACIÓN BIOMÉTRICA                      │
└─────────────────────────────────────────────────────────────────────────┘

1️⃣ USUARIO COMPLETA DATOS
   ┌──────────────┐
   │   Usuario    │
   │  completa    │
   │  formulario  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │   Selecciona │
   │ consentimien-│
   │     tos      │
   └──────┬───────┘
          │
          ▼

2️⃣ SOLICITA VERIFICACIÓN BIOMÉTRICA
   ┌──────────────────────────────────────┐
   │  Frontend: registrarConsentimientos  │
   │  POST /api/formularios/publico/      │
   │       {TOKEN}/registrar              │
   └──────────────┬───────────────────────┘
                  │
                  ▼
   ┌──────────────────────────────────────┐
   │  Backend: Crea sesión DIDIT          │
   │  - Genera verification_token         │
   │  - Llama API DIDIT                   │
   │  - Configura callback URL:           │
   │    https://dominio.com/formulario/   │
   │    publico/{TOKEN}?                  │
   │    didit_return=true&                │
   │    verification_token={VT}&          │
   │    status={STATUS}&                  │
   │    verified={VERIFIED}               │
   └──────────────┬───────────────────────┘
                  │
                  ▼
   ┌──────────────────────────────────────┐
   │  Backend responde:                   │
   │  {                                   │
   │    "tipo_verificacion": "biometria", │
   │    "verification_url": "...",        │
   │    "session_id": "...",              │
   │    "token_verificacion": "..."       │
   │  }                                   │
   └──────────────┬───────────────────────┘
                  │
                  ▼

3️⃣ REDIRIGE A DIDIT
   ┌──────────────────────────────────────┐
   │  Frontend detecta verification_url   │
   │  window.location.href =              │
   │    response.verification_url         │
   └──────────────┬───────────────────────┘
                  │
                  ▼
   ┌──────────────────────────────────────┐
   │         USUARIO EN DIDIT             │
   │  ┌────────────────────────────────┐  │
   │  │  1. Toma foto de cédula        │  │
   │  │  2. Toma selfie                │  │
   │  │  3. DIDIT procesa y verifica   │  │
   │  └────────────────────────────────┘  │
   └──────────────┬───────────────────────┘
                  │
                  ▼

4️⃣ DIDIT REDIRIGE DE VUELTA
   ┌──────────────────────────────────────┐
   │  DIDIT redirige a callback URL       │
   │  https://dominio.com/formulario/     │
   │  publico/ABC123?                     │
   │  didit_return=true&                  │
   │  verification_token=XYZ789&          │
   │  status=success&                     │
   │  verified=true                       │
   └──────────────┬───────────────────────┘
                  │
                  ▼

5️⃣ FRONTEND DETECTA RETORNO
   ┌──────────────────────────────────────┐
   │  ngOnInit() ejecuta:                 │
   │  checkDiditCallback()                │
   │                                      │
   │  Lee parámetros URL:                 │
   │  - didit_return                      │
   │  - verification_token                │
   │  - status                            │
   │  - verified                          │
   └──────────────┬───────────────────────┘
                  │
                  ▼

6️⃣ PROCESA RESULTADO
   ┌──────────────────────────────────────┐
   │  manejarRegresoDeDidit()             │
   │                                      │
   │  ¿status=success Y verified=true?    │
   └──────────┬───────────┬───────────────┘
              │           │
         SÍ   │           │  NO
              ▼           ▼
   ┌──────────────┐  ┌──────────────┐
   │  ✅ ÉXITO    │  │  ❌ ERROR    │
   │              │  │              │
   │  Marca como  │  │  Muestra     │
   │  verificado  │  │  mensaje     │
   │              │  │  de error    │
   │  Ejecuta:    │  │              │
   │  completar   │  └──────────────┘
   │  Consentimien│
   │  tos()       │
   └──────┬───────┘
          │
          ▼

7️⃣ COMPLETA PROCESO
   ┌──────────────────────────────────────┐
   │  completarConsentimientos()          │
   │  POST /api/formularios/publico/      │
   │       {TOKEN}/registrar              │
   │                                      │
   │  Backend:                            │
   │  - Guarda consentimientos            │
   │  - Genera certificados PDF           │
   │  - Envía email con documentos        │
   └──────────────┬───────────────────────┘
                  │
                  ▼
   ┌──────────────────────────────────────┐
   │  ✅ PROCESO COMPLETADO               │
   │                                      │
   │  Muestra sección de finalización     │
   │  con enlaces a:                      │
   │  - Certificado de consentimientos    │
   │  - Términos y condiciones            │
   └──────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════

🔍 PUNTOS CRÍTICOS A VERIFICAR:

┌─────────────────────────────────────────────────────────────────────────┐
│ PUNTO 1: Backend devuelve verification_url                              │
│ ────────────────────────────────────────────────────────────────────────│
│ Verificar en consola del navegador:                                     │
│ console.log('Respuesta registro:', response);                           │
│                                                                          │
│ Debe contener:                                                           │
│ ✓ verification_url                                                       │
│ ✓ session_id                                                             │
│ ✓ token_verificacion                                                     │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ PUNTO 2: Backend configura callback URL en DIDIT                        │
│ ────────────────────────────────────────────────────────────────────────│
│ El backend debe enviar a DIDIT la URL de retorno:                       │
│                                                                          │
│ callback_url = "https://TU_DOMINIO/formulario/publico/{TOKEN}?" +       │
│                "didit_return=true&" +                                    │
│                "verification_token={VERIFICATION_TOKEN}&" +              │
│                "status={STATUS}&" +                                      │
│                "verified={VERIFIED}"                                     │
│                                                                          │
│ DIDIT reemplazará {STATUS} y {VERIFIED} con valores reales              │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ PUNTO 3: DIDIT redirige con parámetros correctos                        │
│ ────────────────────────────────────────────────────────────────────────│
│ Después de completar verificación, la URL debe ser:                     │
│                                                                          │
│ https://dominio.com/formulario/publico/ABC123?                          │
│   didit_return=true&                                                     │
│   verification_token=XYZ789&                                             │
│   status=success&                                                        │
│   verified=true                                                          │
│                                                                          │
│ Verificar en la barra de direcciones del navegador                      │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ PUNTO 4: Frontend detecta y procesa parámetros                          │
│ ────────────────────────────────────────────────────────────────────────│
│ El método checkDiditCallback() se ejecuta en ngOnInit()                 │
│                                                                          │
│ Verificar en consola:                                                    │
│ - "Verificación biométrica completada" (éxito)                          │
│ - "Verificación biométrica fallida" (error)                             │
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

🛠️ DEBUGGING:

Para verificar dónde está el problema, agrega estos console.log:

1. En solicitarCodigo() - línea 538:
   console.log('📤 Enviando solicitud de verificación biométrica');

2. Después de recibir respuesta - línea 544:
   console.log('📥 Respuesta del backend:', response);
   console.log('🔗 verification_url:', response.verification_url);
   console.log('🆔 session_id:', response.session_id);

3. En checkDiditCallback() - línea 747:
   console.log('🔍 Verificando callback de DIDIT');
   console.log('📍 URL actual:', window.location.href);
   console.log('📋 Parámetros:', {
     diditReturn,
     verificationToken,
     status,
     verified
   });

4. En manejarRegresoDeDidit() - línea 759:
   console.log('🎯 Procesando regreso de DIDIT');
   console.log('✅ Es exitoso:', esExitoso);
```
